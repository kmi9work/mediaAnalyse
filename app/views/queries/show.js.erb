$("#texts").html("<%= escape_javascript(render 'show') %>");
$("#specific_js").html("\
  $('#queries_accord').ready(function() {\
    $('#queries_accord').accordion( 'option', 'active', <%= @queries.index(@query) %> );\
  });")
history.pushState(null, "", '<%= request.original_fullpath %>');