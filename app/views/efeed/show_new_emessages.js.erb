$('#new_rows').hide();
$("hr").remove();
$(".article").css('background-color', '#03004d');
$('#texts').prepend('<hr>');

<% @texts.reverse_each do |text| %>
  <% if session[:estyle] == "blue" %>
    $('#texts').prepend('<%= j render "text", text: text, background_color: "#03004d", color: "#00c2a8"%>');
  <% else %>
    $('#texts').prepend('<%= j render "text", text: text, background_color: "#e3ddb8", color: "#000" %>');
  <% end %>
  
<% end %>