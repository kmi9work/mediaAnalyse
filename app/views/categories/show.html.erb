<% if @categories %>
	<%= content_for :categories do %>
		<%= render partial: 'categories', locals: {categories: @categories, not_new: true} %>
	<% end %>
<% end %>

<%= content_for :queries do %>
  <%= render partial: 'queries', locals: {queries: @queries, category_id: params[:id], not_new: true} %>
<% end %>