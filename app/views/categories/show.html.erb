<% if @categories %>
	<%= content_for :categories do %>
		<%= render partial: 'categories/categories', locals: {not_new: true} %>
	<% end %>
<% end %>

<%= content_for :queries do %>
  <%= render partial: 'categories/queries', locals: {queries: @queries, category_id: params[:id], not_new: true} %>
<% end %>