$('#tracking').attr('value', "Отключить трэкинг");
$('#tracking').closest("form").attr('action', "<%= disable_tracking_path %>");
window.track_on = true;
$(function() {
  return setTimeout(getNewLinks, 3000);
});